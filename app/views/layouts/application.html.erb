<!doctype html>
<html lang="en">
<head>
  <%= render 'layouts/partials/head' %>
</head>
<body>
  <div class="container">
    <h3 class="text-info">Book search app</h3>
    <hr>
    <div class="row">
      <div class="col-lg-12">
        <h4 class="text-muted">Search ISBN</h4>
      </div>
      <div class="col-lg-11">
        <input class="form-control" placeholder="Search book by isbn. Type ISBN number here" autofocus="true">
      </div>
      <div class="col-lg-1">
        <a class="btn btn-default">
          <span class="glyphicon glyphicon-search" aria-hidden="true"></span> Search
        </a>
      </div>
    </div>
    <hr>
    <div class="row">
      <div class="well bg-info">
        <% @recent_searches.each do |recent_search| %>
          <img src="<%= recent_search.thumbnail_url %>">
          <p class="text-muted">isbn: <%= recent_search.isbn %></p>
          <p class="text-info">
            <%= recent_search.book_name %> by: <%= recent_search.author %>
          </p>
        <% end %>
      </div>
    </div>
  </div>
  <%= render 'layouts/partials/footer' %>
</body>
</html>
